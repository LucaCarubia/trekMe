{% extends 'layout.html.twig' %}

{% block content %}
    {% for trek in treks %}
        <div class="card" style="width: 18rem;">
            <img src="..." class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{ trek.title }}</h5>
                <p class="card-text">{{ trek.content }}</p>
                <a href="#" class="btn btn-primary">Lire la suite</a>
            </div>
        </div>
    {% endfor %}
{% endblock %}